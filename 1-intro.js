const amount = 12
if (amount > 10) {
  console.log('large number')
} else {
  console.log('small number')
}

console.log(`Hey this is our first node project`)

/*
    In vanillaJS we have access to the window objects, but in node.js we do not access 
    to the window object because there is no browser in node.js, but there are some globals


*/
